# GraphRox (C and Python bindings)

This crate is used for generating a C dynamic link library from the Rust library in
`graphrox-rs`. The Python wrapper can use the C library to call GraphRox functions.
